[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "SolarThing"
authors = [{name = "Joshua Shannon", email = "retrodaredevil@gmail.com"}]
dynamic = ["version", "description"]
